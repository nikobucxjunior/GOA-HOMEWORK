// Example 1:
const squares = [1, 2, 3, 4].map(item => item ** 2);
console.log(squares); // Output: [1, 4, 9, 16]

// Example 2:
const strings = ["hello", "world"].map(item => item.toUpperCase());
console.log(strings); // Output: ["HELLO", "WORLD"]
