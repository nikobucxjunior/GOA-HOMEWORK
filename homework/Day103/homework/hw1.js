// Example 1:
const evenNumbers = [1, 2, 3, 4, 5, 6].filter(item => item % 2 === 0);
console.log(evenNumbers); // Output: [2, 4, 6]

// Example 2:
const longStrings = ["hi", "hello", "world", "yes"].filter(item => item.length > 3);
console.log(longStrings); // Output: ["hello", "world"]
